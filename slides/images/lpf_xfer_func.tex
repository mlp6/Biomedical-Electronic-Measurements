\documentclass{standalone}
\usepackage[siunitx,american,RPvoltages]{circuitikz}
\begin{document}
\begin{circuitikz}
    \begin{tikzpicture}
        % Draw x-axis
        \draw[->] (0,0) -- (9,0) node[below] {$\omega$};
        % Draw y-axis
        \draw[->] (0,0) -- (0,1.75) node[left] {$|H(\omega)|$};
        % Plot the low-pass filter curve (approximated with points)
        \draw[thick, blue] plot[domain=0:6, samples=100] (\x, {1 / sqrt(1 + (\x/2)^2)});
        % Annotate cutoff frequency ω₀ at ω=2 (assuming ω₀=2 for example)
        \draw[dashed] (2, {1 / sqrt(1 + (2/2)^2)}) -- (2,0) node[below] {$\omega_o$};
        \draw[dashed] (0, {1 / sqrt(1 + (2/2)^2)}) -- (2, {1 / sqrt(1 + (2/2)^2)});
        % Add y-axis tick at 1/sqrt(2)
        \draw (0, {1/sqrt(2)}) -- (-0.1, {1/sqrt(2)}) node[left] {$\frac{1}{\sqrt{2}}$};

        \draw (0, 1) -- (-0.1, 1) node[left, yshift=2mm] {1.0};
        
        % Extend bounding box upwards to avoid cutting off the label
        \path (0,3.0);
        \path (12,0);
        
        
    \end{tikzpicture}
\end{circuitikz}
\end{document}